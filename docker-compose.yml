services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - STORAGE_TYPE=local
      - STORAGE_LOCAL_PATH=/data/bundles
      - AUTH_SECRET=change-me-to-a-random-secret
