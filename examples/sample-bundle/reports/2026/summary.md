# Nested Report

This file lives under `reports/2026/` so the bundle tree shows nested
directories. It is linked from the landing page with a relative markdown link.

## Observations

- The sample bundle contains multiple file types.
- The file viewer can open markdown, logs, JSON, and images.
- Relative links are resolved from the current markdown file.
